name: iOS build and tests
on:
  push:
    branches:
      - main
  pull_request:
  workflow_dispatch:
permissions:
  contents: read
jobs:
  test:
    runs-on: macos-26
    timeout-minutes: 25
    env:
      DEVELOPER_DIR: /Applications/Xcode_26.4.app/Contents/Developer
    steps:
      - uses: actions/checkout@11d5960a326750d5838078e36cf38b85af677262 # v4
      - name: Resolve and verify locked dependencies
        run: |
          xcodebuild -resolvePackageDependencies \
            -project shapes-app/ApproovShapes.xcodeproj -scheme ApproovShapes \
            -derivedDataPath build -onlyUsePackageVersionsFromResolvedFile
          python3 scripts/verify-dependencies.py build
          git diff --exit-code -- shapes-app/ApproovShapes.xcodeproj/project.xcworkspace/xcshareddata/swiftpm/Package.resolved
      - name: Test the sample on iOS Simulator
        run: |
          xcodebuild -version
          xcodebuild -project shapes-app/ApproovShapes.xcodeproj \
            -scheme ApproovShapes \
            -destination 'platform=iOS Simulator,name=iPhone 17,OS=26.4.1' \
            -derivedDataPath build -resultBundlePath TestResults.xcresult \
            -disableAutomaticPackageResolution CODE_SIGN_IDENTITY=- test
      - name: Build Release for iOS devices
        run: |
          xcodebuild -project shapes-app/ApproovShapes.xcodeproj \
            -scheme ApproovShapes -configuration Release \
            -destination 'generic/platform=iOS' -derivedDataPath build \
            -disableAutomaticPackageResolution CODE_SIGNING_ALLOWED=NO build
      - name: Save test results
        if: always()
        uses: actions/upload-artifact@ea165f8d65b6e75b540449e92b4886f43607fa02 # v4
        with:
          name: ios-test-results
          path: TestResults.xcresult
          if-no-files-found: ignore
